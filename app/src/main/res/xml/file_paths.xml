<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Maps to Context.getExternalFilesDir(Environment.DIRECTORY_DOWNLOADS) -->
    <external-files-path name="apk_downloads" path="Downloads/" />
    <!-- Downloaded documents for passport viewer -->
    <cache-path name="documents" path="documents/" />
    <!-- Worker photo captures from camera -->
    <cache-path name="photos" path="photos/" />
    <!-- HSE observation photos (internal storage) -->
    <files-path name="observations" path="observations/" />
</paths>
